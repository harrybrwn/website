on:
  - push
  - pull_request
name: lint
jobs:
  lint:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v2
        with:
          go-version: ~1.17
      - run: go vet ./app/... ./pkg/...
