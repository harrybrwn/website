version: "3.8"

services:
  nginx:
    volumes:
      - ./build:/var/www/harrybrwn.com:ro
      - ./config/nginx:/etc/nginx:ro
  api:
    command: -p 8081 -d
    volumes:
      - ./build:/app/build:ro
