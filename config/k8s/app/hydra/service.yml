apiVersion: v1
kind: Service
metadata:
  name: hydra
spec:
  selector:
    app: hydra
  ports:
  - port: 4444
    targetPort: public
    name: hydra-public
  - port: 4445
    targetPort: admin
    name: hydra-admin
