bases:
  - github.com/kubernetes-sigs/nfs-subdir-external-provisioner/deploy

patchesStrategicMerge:
  - nfs-provisioner-patch.yml

patches:
  - target:
      kind: StorageClass
      name: nfs-client
    patch: |-
      - op: replace
        path: /metadata/name
        value: nfs-client-storage
