bases:
  - ../app

# resources:
#   - provision-job.yml

secretGenerator:
  - name: nginx-secrets
    behavior: replace
    files:
      - certs/harrybrwn.com.key
      - certs/harrybrwn.com.crt
      - certs/hrry.me.key
      - certs/hrry.me.crt
      - certs/hrry.dev.key
      - certs/hrry.dev.crt
  # TODO uncomment when adding the container registry
  #- name: registry-secrets
  #  behavior: replace
  #  files:
  #    - registry/htpasswd