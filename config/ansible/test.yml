- name: Random Tests
  hosts: localhost
  # hosts: stg
  # become: true
  vars_files:
    - vars/become-password.yml
  become: false
  gather_facts: false
  # tasks:
  #   - debug: var=ansible_user
  #   - shell: ls -la
  #     register: a
  #   - debug: var=a.stdout_lines

# - name: Configure Router
#   become: false
#   hosts: router
#   gather_facts: false
#   tasks:
#     # - name: Upload NAT Rules script
#     #   copy:
#     #     src: files/nat-rules.rsc
#     #     dest: disk1/nat-rules.rsc
#     - debug:
#         var: ansible_host
#     #   delegate_to: localhost
#     # - shell: ssh admin@router.lan '/system resource print'
#     #   delegate_to: localhost
