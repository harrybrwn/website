server {
	server_name registry.harrybrwn.com
				registry.harrybrwn.local;
	listen      443 ssl http2;
	listen [::]:443 ssl http2;

	# set $registry registry:5000;
	# set $registry localhost:5000;
	set $registry 10.0.0.101:5000;

	location / {
		proxy_pass https://$registry;
	}
}